"use client";
import { FormEvent, useState } from "react";
import { useRouter } from "next/navigation";
import { getSupabaseBrowserClient } from "@/lib/supabase/client";
export function ResetPasswordForm(){const router=useRouter();const[pending,setPending]=useState(false);const[message,setMessage]=useState("");async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();setMessage("");const form=new FormData(event.currentTarget);const password=String(form.get("password")??"");const confirmation=String(form.get("confirmation")??"");if(password.length<8)return setMessage("A nova senha precisa ter pelo menos 8 caracteres.");if(password!==confirmation)return setMessage("As senhas não são iguais. Confira e tente novamente.");const supabase=getSupabaseBrowserClient();if(!supabase)return setMessage("Não foi possível conectar ao sistema agora.");setPending(true);const{error}=await supabase.auth.updateUser({password});if(error){setPending(false);return setMessage("O link pode ter expirado. Solicite uma nova recuperação.");}await supabase.auth.signOut();router.replace("/login?mode=login&reset=success");router.refresh();}return <form className="auth-form reset-password-form" onSubmit={submit}><label>Nova senha<input name="password" type="password" autoComplete="new-password" minLength={8} required placeholder="Mínimo de 8 caracteres"/></label><label>Confirmar nova senha<input name="confirmation" type="password" autoComplete="new-password" minLength={8} required placeholder="Digite novamente"/></label>{message&&<p className="auth-message error" role="alert">{message}</p>}<button className="button auth-submit" type="submit" disabled={pending}>{pending?"Alterando senha...":"Cadastrar nova senha →"}</button></form>}
